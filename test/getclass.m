function index = getclass(str,strs)

    ind=find(ismember(strs,'KU'))

if isstring(arg)
    for index=1:length(strs)
        if strcmp(arg,strs{index})
            return;
        end
        error('Internal error. Contact developers');
    end
else
    
    